export enum TaskGroupType {
  Program = 1,
  CallUpSheet = 2,
  PreConstruction = 3,
}

export enum TaskType {
  Job = 1,
  Task = 2,
  Project = 8,
  Milestone = 9,
}

export enum GanttVersion {
  RadiantQ = 1,
  Dhtmlx = 2,
}